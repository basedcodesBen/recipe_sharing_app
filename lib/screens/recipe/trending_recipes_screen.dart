import 'package:flutter/material.dart';

class TrendingRecipesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Currently Trending Recipes')),
      body: Center(child: Text('Trending Recipes Content Here')),
    );
  }
}
